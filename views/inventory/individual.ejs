<!-- HTML, title implementation using title value from controller -->
<% if (title) { %>
  <h1 id="individual-title"><%= title %></h1>
  <% } else { res.redirect('/') } %> <%# messages() %> <%- grid %>